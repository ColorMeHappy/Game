name: LexiFrance one-time Quiz editorial final
on:
  push:
    branches:
      - lexifrance-v5-solve2
    paths:
      - 'legal-app/editorial-final.mjs'
      - '.github/workflows/lexifrance-editorial-final.yml'
permissions:
  contents: write
jobs:
  migrate:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node legal-app/editorial-final.mjs
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add legal-app/content/quizzes/*.json
          git diff --cached --quiet && exit 0
          git commit -m "Apply final Quiz editorial pass"
          git push
