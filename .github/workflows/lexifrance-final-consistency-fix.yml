name: LexiFrance one-time final QA consistency patch
on:
  push:
    branches:
      - lexifrance-v5-solve2
    paths:
      - 'legal-app/final-consistency-fix.mjs'
      - '.github/workflows/lexifrance-final-consistency-fix.yml'
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node legal-app/final-consistency-fix.mjs
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add legal-app/js/lesson.js legal-app/e2e.mjs
          git diff --cached --quiet && exit 0
          git commit -m "Align spaced review and Profile QA semantics"
          git push
