import{lesson}from'./data.js';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export async function enhanceLesson(id){let data;try{data=await lesson(id)}catch{return}if(!data.deadlineTable?.rows?.length&&!data.criticalWarning)return;const modals=[...document.querySelectorAll('.modal')],modal=modals.at(-1);if(!modal)return;const body=modal.querySelector('.modal-body');if(!body||body.querySelector('[data-legal-enhancement]'))return;const anchor=body.querySelector('.comparison-list')?.closest('.legal-card')||body.querySelector('.source-box');const wrap=document.createElement('section');wrap.className='section';wrap.dataset.legalEnhancement='1';let html='';if(data.criticalWarning)html+=`<div class="legal-warning"><b>Важно</b><br>${esc(data.criticalWarning)}</div>`;if(data.deadlineTable?.rows?.length){html+=`<div class="tag">${esc(data.deadlineTable.title||'Сроки процедуры')}</div><div class="deadline-wrap"><table class="deadline-table"><thead><tr>${(data.deadlineTable.columns||[]).map(c=>`<th>${esc(c)}</th>`).join('')}</tr></thead><tbody>${data.deadlineTable.rows.map(r=>`<tr><td>${esc(r.situation)}</td><td>${esc(r.filing)}</td><td>${esc(r.start)}</td><td>${esc(r.judgment)}</td><td>${esc(r.notes)}</td><td><a href="${r.sourceUrl}" target="_blank" rel="noopener">${esc(r.sourceName||'Official')} ↗</a></td></tr>`).join('')}</tbody></table></div>`}wrap.innerHTML=html;if(anchor)body.insertBefore(wrap,anchor);else body.appendChild(wrap)}