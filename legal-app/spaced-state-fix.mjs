import fs from'node:fs';const p='legal-app/js/state.js';let s=fs.readFileSync(p,'utf8');const a="export function recordQuizReview(lessonId,result){const p=lessonProgress(lessonId);if(masteryFrom(p)<100)return{recorded:false,due:false};const freshness=knowledgeFreshness(lessonId),due=",b="export function recordQuizReview(lessonId,result){if(mastery(lessonId).score<100)return{recorded:false,due:false};const freshness=knowledgeFreshness(lessonId),p=lessonProgress(lessonId),due=";if(!s.includes(a))throw new Error('recordQuizReview marker not found');s=s.replace(a,b);fs.writeFileSync(p,s);console.log('Fixed stale lesson progress reference in recordQuizReview');