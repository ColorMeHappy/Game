import fs from'node:fs';
for(const p of['legal-app/js/lesson.js','legal-app/e2e.mjs']){let s=fs.readFileSync(p,'utf8');if(p.endsWith('lesson.js')){const a="weakTopics:weak.map(([target,label])=>({target,label}))});",b="weakTopics:weak.map(([target,label])=>({target,label})),dueAtStart:!!run.reviewDueAtStart});";if(!s.includes(a))throw new Error('lesson marker not found');s=s.replace(a,b)}else{const a="check((await page.locator('body').innerText()).includes('Application'),'Profile separates Knowledge and Application');",b="check((await page.locator('body').innerText()).toLowerCase().includes('application'),'Profile separates Knowledge and Application');";if(!s.includes(a))throw new Error('e2e marker not found');s=s.replace(a,b)}fs.writeFileSync(p,s)}console.log('Patched spaced review handoff and capitalization-safe Profile QA');