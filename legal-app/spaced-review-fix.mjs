import fs from'node:fs';const p='legal-app/js/lesson.js';let s=fs.readFileSync(p,'utf8');const a="startingMastery:mastery(l.id).score});",b="startingMastery:mastery(l.id).score,reviewDueAtStart:mastery(l.id).score===100&&knowledgeFreshness(l.id).due});";const c="if(run.startingMastery===100)review=recordQuizReview",d="if(run.reviewDueAtStart||(run.startingMastery===100&&knowledgeFreshness(l.id).due))review=recordQuizReview";if(!s.includes(a)||!s.includes(c))throw new Error('lesson markers not found');s=s.replace(a,b).replace(c,d);fs.writeFileSync(p,s);console.log('Hardened spaced-review run semantics');